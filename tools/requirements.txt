# TTE Animation Tools - Optional Dependencies
# Install with: pip install -r tools/requirements.txt

# Terminal Text Effects library
terminaltexteffects>=0.14.0

# Textual TUI framework (if not already installed)
textual>=0.47.0

# Rich console (usually bundled with Textual)
rich>=13.0.0
