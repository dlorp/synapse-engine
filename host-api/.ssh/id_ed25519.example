# SSH Key Placeholder
#
# Generate your own SSH key for host automation:
#
#   ssh-keygen -t ed25519 -f ~/.ssh/synapse_host_api -N ""
#
# Then copy it to this directory:
#
#   cp ~/.ssh/synapse_host_api host-api/.ssh/id_ed25519
#
# NEVER commit actual SSH private keys to version control!
